# Gazette Generation API

- `POST /api/v1/gazettes/generate` builds class, section, program, or overall gazettes from result and grade calculations.
- `GET /api/v1/gazettes/` lists generated gazettes.

Gazettes include pass/fail summaries and ranking by GPA when available, otherwise percentage.
