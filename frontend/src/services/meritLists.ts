export interface MeritPayload { exam_id: string; scope_type: 'institution'|'program'|'class'|'section'|'session'; scope_id?: string; basis: 'percentage'|'gpa'; title: string; tie_breakers?: string[]; publish_immediately?: boolean; }
const API_BASE = import.meta.env.VITE_API_BASE_URL ?? 'http://localhost:8000/api/v1';
async function request(path: string, options?: RequestInit) { const response = await fetch(`${API_BASE}${path}`, options); if (!response.ok) throw new Error(`Merit list request failed: ${response.status}`); return response.json(); }
export const meritListsApi = { list: (query='') => request(`/merit-lists/${query}`), generate: (payload: MeritPayload) => request('/merit-lists/generate', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) }), publish: (id:string) => request(`/merit-lists/${id}/publish`, { method:'POST' }), lock: (id:string) => request(`/merit-lists/${id}/lock`, { method:'POST' }), certificate: (itemId:string) => request(`/merit-lists/items/${itemId}/certificate`, { method:'POST' }) };
